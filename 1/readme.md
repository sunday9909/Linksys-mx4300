
# =================================================================
# Linksys MX4300 (LN1301) mtd30 扩容 OpenClash 一键优化版
# =================================================================
复制onekey代码

请直接在 SSH 终端复制并粘贴以下完整指令：

交互式确认：脚本开始前会要求输入 y，防止误操作。
写入权限校验：在搬家数据前，先用 touch 命令在 MTD30 挂载点创建隐藏文件，只有测试通过才会继续，确保 OpenClash 以后能存下内核。
安装失败报错：如果 opkg 因为断网失败，脚本会报错并停止，不再往下乱跑。
